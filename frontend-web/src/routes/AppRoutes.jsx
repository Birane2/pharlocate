import { BrowserRouter } from "react-router-dom";
import App from "../App";

function AppRoutes() {
  return (
    <BrowserRouter>
      <App />
    </BrowserRouter>
  );
}

export default AppRoutes;
